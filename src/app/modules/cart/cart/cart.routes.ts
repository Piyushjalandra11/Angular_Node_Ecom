// src/app/modules/cart/cart.routes.ts
import { Routes } from '@angular/router';
import { CartComponent } from './cart.component'; // Assuming you have this component

export const cartRoutes: Routes = [
  { path: '', component: CartComponent },
];
